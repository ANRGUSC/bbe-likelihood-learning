# Contributing

Keep changes small, CPU-friendly, and dependency-light. Run `pytest` and
`python examples/quick_demo.py` before opening a pull request. New simulator
or receiver backends must preserve the action-conditioned measurement interface.
